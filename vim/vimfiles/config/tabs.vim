" The width of the tab character
" Supposeduly this should be set to 8, 
" I'm setting it to 4 so that tabs appear as 4
set tabstop=4

" The width of an indent, e.g. with >, <, = commands
set shiftwidth=4

" Make sure only spaces are inserted with a tab in insert mode,
" not a combination of tabs and spaces
set softtabstop=0

" Make the tab key in insert mode insert spaces instead of tabs
set expandtab

" Use shiftwidth space for tabs
set smarttab
